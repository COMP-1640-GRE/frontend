export enum UserRole {
  GUEST = "guest",
  STUDENT = "student",
  ADMIN = "administrator",
  MANAGER = "university_marketing_manager",
  COORDINATOR = "faculty_marketing_coordinator",
}

export enum AccountStatus {
  LOCKED = "locked",
  ACTIVE = "active",
  INACTIVE = "inactive",
}
